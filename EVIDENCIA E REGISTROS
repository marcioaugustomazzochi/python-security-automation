# Automação de Segurança em Python

Projeto focado na automação de processos de segurança da informação utilizando Python.

Funcionalidades principais:

- Varredura de vulnerabilidades em sistemas e redes
- Coleta de dados de ameaças de fontes públicas
- Análise de tráfego de rede para identificação de atividades suspeitas

O objetivo é apoiar análises técnicas e resposta a incidentes, respeitando boas práticas de privacidade, LGPD e conformidade.

---

## Funcionalidades

### Varredura de Vulnerabilidades com Nmap

Automatiza o uso do Nmap para identificar serviços, versões e possíveis vulnerabilidades em hosts.

### Coleta de Dados de Ameaças

Scripts para coleta e análise de dados de fontes públicas confiáveis, como bases de CVE.

### Análise de Tráfego de Rede

Captura e análise básica de pacotes de rede em tempo real utilizando Python.

---

## Tecnologias Utilizadas

- Python 3.x
- Nmap
- Scapy
- Requests

---

## Instalação

```bash
git clone https://github.com/marcioaugustomazzochi/python-security-automation.git
cd python-security-automation
pip install -r requirements.txt
Exemplos de Uso
Varredura de Vulnerabilidades com Nmap
import subprocess

def run_nmap(target):
    command = f"nmap -sV {target}"
    result = subprocess.run(
        command,
        shell=True,
        capture_output=True,
        text=True
    )
    print(result.stdout)

run_nmap("192.168.56.124")
Coleta de Dados de Ameaças (CVE)
import requests

def get_cve_data():
    url = "https://cve.circl.lu/api/last"
    response = requests.get(url)
    data = response.json()

    for entry in data:
        print(f"CVE ID: {entry['id']} | Descrição: {entry['summary']}")

get_cve_data()
Roadmap
Varredura de vulnerabilidades com Nmap

Coleta de dados de ameaças via API de CVE

Análise de tráfego com Scapy

Geração automática de relatórios

Licença
Este projeto está licenciado sob a MIT License.
Consulte o arquivo LICENSE para mais detalhes.

Aviso Ético
Todos os scripts devem ser utilizados exclusivamente em ambientes controlados e autorizados.

A aplicação destas técnicas em redes ou sistemas sem permissão prévia é ilegal.


---
